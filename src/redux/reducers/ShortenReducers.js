const intialState = {
    urls:[],
   
}

export const Shortenreducers = (state=intialState,actions)=>{
        switch(actions.type) {
            case "ADD":
                return state
            default:
                return state
        }
}